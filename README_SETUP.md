# 🚀 Aurora Launcher - Настройка для ThunderBolt

## ✅ Что уже сделано

1. ✅ Установлен Aurora Launcher
2. ✅ Настроена MySQL авторизация
3. ✅ Подключена база данных `s309042_REGISTRATION`
4. ✅ Настроены столбцы БД (uuid, username, password, accessToken, serverID)
5. ✅ Настроен SHA256 хеширование паролей
6. ✅ Порт API: 9274
7. ✅ Клиентский лаунчер настроен на ваш сервер
8. ✅ Профиль клиента создан: `ThunderBolt.json`

## 📋 Текущая конфигурация

**Файл:** `/home/ubuntu/aurora/packages/server/dist/LauncherServerConfig.hjson`

**База данных:**
- Host: `213.171.18.137:3306`
- Database: `s309042_REGISTRATION`
- Table: `users`

**Столбцы:**
- `uuid` - UUID пользователя
- `username` - Логин
- `password` - Пароль (SHA256)
- `accessToken` - Токен доступа
- `serverID` - ID сервера
- `skinUrl` - URL скина (нужно добавить в БД если нет)
- `capeUrl` - URL плаща (нужно добавить в БД если нет)

## 🔧 Что нужно сделать дальше

### 1. Проверить столбцы в БД

Проверьте, есть ли в таблице `users` столбцы `skinUrl` и `capeUrl`:

```sql
DESCRIBE users;
```

Если их нет, добавьте:

```sql
ALTER TABLE users ADD COLUMN skinUrl VARCHAR(255) DEFAULT NULL;
ALTER TABLE users ADD COLUMN capeUrl VARCHAR(255) DEFAULT NULL;
```

### 2. Интегрировать API скинов

У вас есть API для скинов: `http://145.239.86.211/skins/get.php?type=skin&username=%username%`

Нужно будет модифицировать `DatabaseAuthProvider` чтобы он:
- При запросе профиля подставлял URL скина из вашего API
- Или хранил в БД ссылку на ваш API

### 3. Настроить профили клиентов

✅ Профиль создан в `profiles/ThunderBolt.json`

### 4. Собрать .exe для игроков

📦 **См. файл:** `ИНСТРУКЦИЯ_СБОРКИ_EXE.md`

Коротко:
- **Вариант 1 (рекомендуется):** Использовать GitHub Actions - автоматическая сборка
- **Вариант 2:** Собрать на Windows машине
- **Вариант 3:** Использовать готовый Aurora Launcher и изменить URL

## 🚀 Запуск сервера

```bash
cd /home/ubuntu/aurora/packages/server
npm run build:dev
node dist/LauncherServer.js
```

Или в режиме разработки:

```bash
cd /home/ubuntu/aurora/packages/server
npm run dev
```

## 📝 Заметки

- Пароли используют SHA256 хеширование (как в Gravit)
- Авторизация через MySQL БД работает
- Нужно настроить скины через API или БД
- Порт API: 9274 (как в Gravit)
- Лаунчер уже настроен на ваш сервер в `packages/launcher/config.ts`
