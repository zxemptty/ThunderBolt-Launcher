# ✅ Пошаговая инструкция: GitHub Actions

## 📋 Что нужно сделать:

### 1. Создайте свой репозиторий на GitHub

1. Перейдите: https://github.com/new
2. Repository name: `ThunderBolt-Launcher`
3. Выберите **Private** (или Public)
4. **НЕ** ставьте галочки на README, .gitignore, license
5. Нажмите **"Create repository"**

### 2. Измените remote на ваш репозиторий

```bash
cd /home/ubuntu/aurora

# Замените USERNAME на ваш GitHub username
git remote set-url origin https://github.com/USERNAME/ThunderBolt-Launcher.git

# Проверьте:
git remote -v
```

### 3. Подготовьте и загрузите код

```bash
cd /home/ubuntu/aurora

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "ThunderBolt Launcher - настроен для сервера"

# Загрузите на GitHub
git push -u origin master
```

**Если попросит пароль:** используйте Personal Access Token (Settings → Developer settings → Personal access tokens → Generate new token)

### 4. Запустите сборку

1. Откройте ваш репозиторий: https://github.com/USERNAME/ThunderBolt-Launcher
2. Перейдите в **Actions** (вкладка сверху)
3. Слева выберите **"Build Windows Launcher"**
4. Справа нажмите **"Run workflow"**
5. Выберите ветку `master`
6. Нажмите зеленую кнопку **"Run workflow"**

### 5. Дождитесь сборки

- Займет ~10-15 минут
- Вы увидите прогресс в реальном времени
- Когда появится зеленая галочка ✅ - готово!

### 6. Скачайте .exe

1. Нажмите на completed workflow (зеленая галочка)
2. Прокрутите вниз до секции **Artifacts**
3. Скачайте **ThunderBolt-Launcher-Windows**
4. Распакуйте архив
5. Внутри будет `ThunderBolt-Launcher-0.0.4.exe` или похожий файл

## 🎉 Готово!

Теперь у вас есть готовый .exe для раздачи игрокам!

## 🔄 Для следующей сборки:

Просто снова нажмите **"Run workflow"** в Actions - все автоматически!

## ⚠️ Важно:

- Конфиг с паролями MySQL **НЕ** попадет в репозиторий (он в .gitignore)
- При изменениях в коде просто делайте `git push` и workflow запустится автоматически
